import { PaModel } from './twoway.directive';

describe('TwowayDirective', () => {
  it('should create an instance', () => {
    const directive = new PaModel();
    expect(directive).toBeTruthy();
  });
});
