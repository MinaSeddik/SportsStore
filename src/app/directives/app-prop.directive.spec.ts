import { AppPropDirective } from './app-prop.directive';

describe('AppPropDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPropDirective();
    expect(directive).toBeTruthy();
  });
});
