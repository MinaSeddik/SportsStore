import { CellColorDirective } from './cell-color.directive';

describe('CellColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CellColorDirective();
    expect(directive).toBeTruthy();
  });
});
