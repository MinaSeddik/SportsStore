import { PaCellColorSwitcher } from './cell-color-switcher.directive';

describe('CellColorSwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new PaCellColorSwitcher();
    expect(directive).toBeTruthy();
  });
});
