import { PaAttrDirective } from './attr.directive';

describe('AttrDirective', () => {
  it('should create an instance', () => {
    const directive = new PaAttrDirective();
    expect(directive).toBeTruthy();
  });
});
